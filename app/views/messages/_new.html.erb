 <div id = "new-message">
  <%= form_for [@conversation, @message] do |f| %>
    <div>
      <%= f.label :content %>
      <%= f.text_field :content%>
    </div>

    <div>
    <%= f.submit %>
    </div>
  <% end %>
</div>

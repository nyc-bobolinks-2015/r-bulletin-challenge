<h2><%= @topic.name %></h2>
<ul id="conversation-list">
<% @topic.conversations.order(updated_at: :desc).each do |conv| %>
  <li><%= link_to conv.name, conversation_path(conv) %></li>
<% end %>
</ul>

<% if logged_in? %>
  <div>
    <%= render 'conversations/new' %>
  </div>
<% end %>
